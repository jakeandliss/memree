<%= form_for [@title, @entry] do |f| %>
	<p><%= f.text_area :entry, placeholder: "Your entry goes here..."  %></p>
	<p><%= f.file_field :image  %></p>

	<div class="action">
		<%= f.submit 'Save Entry', class: "button" %>
		</div>
	<% end %>