$('#entry_<%= @entry.id %> .header').before('<%= j render(partial: 'edit') %>');
$('#entry_<%= @entry.id %> .header').hide();
$('#entry_<%= @entry.id %> .content').hide();
$('#entry_<%= @entry.id %> .footer').hide();
$('#entry_<%= @entry.id %> .title:hover .delete-x').hide();


    var element = $('#edit_entry_<%= @entry.id %>');
    element.find(".dropzone").dropzone({url: "entries/<%=@entry.id%>/resources", paramName: "resource[avatar]"});

    element.find(".image_click").click(function(event){
      event.preventDefault()
      element.find('.dz-clickable').trigger('click');
    });