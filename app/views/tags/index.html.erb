<div class="row">
	<div class="medium-4 columns">
	 		<%= render 'users/user_sidebar' %>
			<%= render 'users/user_nav' %>  
	</div>
	<div class="medium-8 columns white box-shadow">  
		<div id="journals">
			<h3>My Journals</h3>
			<%= link_to "New Journal", edit_user_path(@user, form: "journals"), id: "add_new_journal", class: "hide" %>
			<%= render 'tags/form' %>
			<div id="journal_added"></div>
			<div id="sub-menu">
				<%= nested_tags @tags %>
			</div>
		</div>
	</div>
</div> 

