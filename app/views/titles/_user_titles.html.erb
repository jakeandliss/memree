<% @titles.each do |t| %>
	<div id="title" class="title">
		<div class="row">
			<div class="medium-9 columns">
				<div id="<%= dom_id(t) %>" class="white box-shadow">
					<div class="header">
						<div id="shared_users">
							<%= image_tag current_user.avatar.url(:avatar) %>
						</div>
						<h3><%= link_to t.title, title_entries_path(t) %></h3>
						<div class="datetime"><h6><u><%= t.title_date.to_date.strftime('%a, %B %e, %Y') if t.title_date.present? %></u></h6>
						</div>
					</div>
					<div class="content clearfix">
						<% t.entries.limit(1).each do |e| %>
								<div class="title_entry">
										<%= truncate_html(e.entry.html_safe, :length => 200, :escape => false) %>
								</div>
						<% end %>
						<div id="entry_image_thumbnails">
							<% t.entries.limit(4).each do |e| %>
								<% next if e.images.blank? %>
									<% e.images.each do |i| %>
									<%= image_tag i.avatar.url(:medium) %>
								<% end %>
							<% end %>
						</div>
					</div>	
					<div class="delete-x"><%= link_to 'x', title_path(t), method: :delete, data: {confirm: 'Are you sure?' }, remote: true %>
					</div>
				</div>
			</div>
			<% if t.all_tags.present? %>
			<div class="medium-3 columns">
				<div class="footer">
					<h5>
						<ul>
							<%= raw tag_links(t.all_tags) %>
						</ul>
					</h5>
				</div>
			</div>
			<% end %>
		</div>
	</div>
<% end %>