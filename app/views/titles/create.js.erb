$('#new_title')[0].reset();
$('.bootstrap-tagsinput .tag [data-role="remove"]').trigger('click');
$('#title_added').hide().prepend('<%= j render(@title) %>').fadeIn(1300);

window.clearAfterSubmit = true;
window.dropzone.removeAllFiles();
window.clearAfterSubmit = false;
