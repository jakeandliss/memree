	</div>
</div>	
<div id="titles">
	<div class="row wrapper">
		<div class="medium-2 columns">
			<div class="user_info">
				<%= image_tag "blank_avatar.png" %>
				<p><%= current_user.first_name  %> <%= current_user.last_name  %></p>
			</div>
			<div class="user_journals">
				<p>Journals</p>
			</div>

		</div>
		<div class="medium-10 columns">
			<div class="form clearfix">
				<%= render 'form' %>
			</div>
			<% @titles.each do |t| %>
				<div class="entry" id="<%= dom_id(t) %>">
					<h3><%= t.title %></h3>
					<p><%= t.title_date %> | <%= link_to 'View Event', title_entries_path(t) %> | <%= link_to 'Delete', title_path(t), method: :delete, data: {confirm: 'Are you sure?' } %> </p>
					<h5>Journals: <%= raw tag_links(t.all_tags) %></h5>
				</div>
				<% end %>
		</div>
	</div>
</div>
 <div class="row">
      <div class="columns">